#include "Rect.h"

// キャラクターの当たり判定用の箱を作って返す
std::list<Rect> Rect::Make_3DBox(std::shared_ptr<Actor> actor) {
	std::list<Rect> result;
	こんなもんか？
	return 
}

// 面の中心座標を返す
Vector3D Rect::Get_Centor_Point(float width, float height, float depth, Vector3D top_right) {
	作る
	return Vector3D();
}
